import { LogInWindow } from "@/components/home/login-window/login-window";

export default function Home() {
  return (
    <div className="min-h-full flex-col relative flex items-center justify-center flex-wrap">
      <LogInWindow />
    </div>
  );
}
